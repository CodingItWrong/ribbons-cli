const centered = {
  textAlign: 'center',
};

const verticalFill = {
  flex: 1,
};

const verticallyCentered = {
  flex: 1,
  justifyContent: 'center',
};

const statusBarMargin = {
  marginTop: 22,
};

export {centered, verticallyCentered, verticalFill, statusBarMargin};
